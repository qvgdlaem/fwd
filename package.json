{
  "name": "fwd",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply fwd --local",
    "db:migrate": "wrangler d1 migrations apply fwd --remote",
    "create-user": "wrangler exec scripts/create-user.ts"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240725.0",
    "tsx": "^4.21.0",
    "typescript": "^5.5.4",
    "wrangler": "4"
  },
  "packageManager": "yarn@1.22.22"
}
